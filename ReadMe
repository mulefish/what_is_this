1. Make sure to have the "switchToAMS.bat" and "larry.zip" files in the same directory on a thumb drive
2. Insert the thumb drive to the targeted machine 
3. Double click "switchToAMS.bat" file to change the app configuration.
